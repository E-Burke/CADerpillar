#include <iostream>
#include "main.h"
#include "debuggerinator9000.h"

int main()
{
    void run(); {
        bool startUp = true;
        return 0;
        if (bool startUp = true) 
        {
            std::cout << "Startup Sequence: Started\n";
            int runtime();
        }
        if (bool runtimeActive = true)
        {
            std::cout << "Runtime: Initiated \n";
        }
    }
};
